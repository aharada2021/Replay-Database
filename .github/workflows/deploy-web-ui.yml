name: Deploy Web UI

on:
  push:
    branches:
      - main
    paths:
      - 'web-ui/**'
      - '.github/workflows/deploy-web-ui.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        type: choice
        options:
          - development
          - production

env:
  CLOUDFRONT_DISTRIBUTION_ID: E312DFOIWOIX5S
  S3_BUCKET_PROD: wows-replay-web-ui-prod
  CUSTOM_DOMAIN: wows-replay.mirage0926.com

jobs:
  deploy-prod:
    name: Deploy to Production (S3 + CloudFront)
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment: production

    defaults:
      run:
        working-directory: web-ui

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: web-ui/package-lock.json

      - name: Install dependencies
        run: rm -f package-lock.json && npm install

      - name: Build Nuxt 3 application
        run: npm run generate

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-1

      - name: Sync to S3
        run: |
          aws s3 sync .output/public/ "s3://${{ env.S3_BUCKET_PROD }}" --delete

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"

      - name: Deployment summary
        run: |
          echo "## Web UI Deployment Summary (Production)" >> $GITHUB_STEP_SUMMARY
          echo "- **Stage**: prod" >> $GITHUB_STEP_SUMMARY
          echo "- **Bucket**: ${{ env.S3_BUCKET_PROD }}" >> $GITHUB_STEP_SUMMARY
          echo "- **CloudFront Distribution**: ${{ env.CLOUDFRONT_DISTRIBUTION_ID }}" >> $GITHUB_STEP_SUMMARY
          echo "- **URL**: https://${{ env.CUSTOM_DOMAIN }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Basic Auth**: Enabled" >> $GITHUB_STEP_SUMMARY
          echo "- **Deployed at**: $(date -u)" >> $GITHUB_STEP_SUMMARY
          echo "- **Git SHA**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
